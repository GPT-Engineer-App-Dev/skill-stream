const Dashboard = () => {
  return (
    <div className="space-y-6">
      <h1 className="text-3xl font-bold">Welcome to CodeLearn</h1>
      <section>
        <h2 className="text-2xl font-semibold">Recent Courses</h2>
        {/* Add content for recent courses */}
      </section>
      <section>
        <h2 className="text-2xl font-semibold">Upcoming Events</h2>
        {/* Add content for upcoming events */}
      </section>
      <section>
        <h2 className="text-2xl font-semibold">Announcements</h2>
        {/* Add content for announcements */}
      </section>
    </div>
  );
};

export default Dashboard;